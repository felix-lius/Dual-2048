# ==============================================================================
# Double 2048: Dual Challenge —— Git 忽略清单
# ==============================================================================
# 这个文件告诉 Git：哪些文件「不要上传到 GitHub」。
# 没有被列在这里的文件，都会正常上传。
#
# 阅读提示（写给非程序员）：
#   - 以 # 开头的行是注释，不起作用，只是解释给人看的。
#   - 一行一条规则；结尾带 / 的表示「整个文件夹」。
#   - 想让某个被忽略的文件恢复上传，把对应那一行删掉即可。
#
# 本项目是纯静态 HTML5 游戏：零 npm 依赖、零构建工具。
# 真正需要上传的是：index.html + src/ + vendor/ + tests/ + docs/ + design/ +
#                   assets/ + preview/ + tools/(脚本)
# 下面列出的都是「本机专属」或「可再生成」的东西，不该进仓库。
# ==============================================================================


# ------------------------------------------------------------------------------
# 1. 本机诊断临时文件【安全红线 —— 最重要的一节，务必保留】
# ------------------------------------------------------------------------------
# 这 4 个 txt 是之前排查「文件被占用」问题时生成的临时记录，其中两个含个人隐私：
#   psproc.txt   —— 本机当时运行的全部进程 + 完整命令行 + 软件安装路径
#                   （含 Adobe、WorkBuddy 等，等于把电脑装了什么全公开）
#   suspects.txt —— QQMusic / Weixin / WPS / NVIDIA 的进程与安装路径，
#                   并且路径里含 Windows 用户名「10955」
#   psdiag.txt   —— 无隐私，但只是几行调试输出，属于无意义残留
#   sync.txt     —— 同上，无隐私的调试残留
#
# 一旦上传到公开仓库，这些信息会被搜索引擎和爬虫永久收录，且 Git 历史很难彻底删干净。
# 所以这里逐个显式列出，确保万无一失。
#
# 【为什么不用 ps*.txt 这种通配写法？】
#   通配符会把未来任何以 ps 开头的 txt 一并吞掉（比如某天你写了 psd-notes.txt
#   或 ps-素材清单.txt 想入库，会莫名其妙上传不了，而且很难发现原因）。
#   这里只有固定的 4 个文件，显式列出更精确、更安全，也更容易看懂。
psdiag.txt
psproc.txt
suspects.txt
sync.txt

# 补充保险：万一以后又生成了类似的进程快照，建议改用统一前缀 _diag- 命名，
# 下面这条会自动忽略它们（当前项目里没有此类文件，不会误伤任何东西）。
_diag-*.txt


# ------------------------------------------------------------------------------
# 2. 构建产物 build/ 【本次的关键决策：不入库】
# ------------------------------------------------------------------------------
# build/ 是 index.html + src/ + vendor/ 的完整拷贝（14 个文件 / 1.4MB），
# 经实际逐字节比对，内容与源码「完全一致」，没有任何编译、压缩或转换。
#
# 决策：不上传。理由有三条 ——
#   (1) 纯重复内容。仓库会凭空多出 1.4MB，且以后每改一次 src/ 里的文件，
#       都要产生「源码 + 拷贝」两份一模一样的改动记录，代码审查时噪音很大，
#       还容易出现「改了 src 忘了同步 build」的版本不一致事故。
#   (2) 重新生成成本几乎为零。需要时一条命令即可（Git Bash 中执行）：
#         rm -rf build && mkdir -p build && cp -r index.html src vendor build/
#   (3) 最关键：入口 index.html 就在仓库根目录，无需额外拷贝即可直接运行。
#       入口 index.html 就在仓库根目录，且它引用资源用的是相对路径
#       （./vendor/phaser.min.js、./src/main.js），无需额外拷贝即可直接
#       以仓库根目录作为站点入口运行。
#
# 【如果你以后改主意，想把 build/ 也传上去】：把下面这一行删掉即可。
build/


# ------------------------------------------------------------------------------
# 3. 打包压缩文件
# ------------------------------------------------------------------------------
# double-2048-v1.0.0.zip（364KB）是早期打的打包文件，已废弃，不再需要。
# 压缩包属于「打包结果」，随时能重新压，不该占用仓库空间和版本历史。
#
# 通配 *.zip 的误伤评估：已全库检查，当前仅存在上述这 1 个 zip 文件，不会误伤。
# 未来若确实需要上传某个 zip（比如给别人的素材包），可以用命令强制添加：
#   git add -f 你的文件名.zip
*.zip


# ------------------------------------------------------------------------------
# 4. 工具脚本产生的本地日志
# ------------------------------------------------------------------------------
# tools/ 里的 Python / Node 脚本（生成封面、图标、录制预览视频）运行时会写日志和
# 中间清单。这些是「上一次在你电脑上跑出来的结果」，对别人没有意义，也会频繁变动。
# 注意：tools/ 下的脚本本身（gen_covers.py / gen_icon.py / record_video.cjs）
#       仍然会正常上传，只忽略它们产生的副产品。
tools/_gen.log
tools/_fonts.txt
*.log


# ------------------------------------------------------------------------------
# 5. AI 协作工作区 .workbuddy/
# ------------------------------------------------------------------------------
# 这里是 AI 协作过程中的工作记忆（每日工作日志），包含内部讨论过程、
# 阶段性结论，以及本机的绝对路径（会暴露 Windows 用户名）。
# 默认不上传：一是含本机路径，二是属于「过程草稿」，与游戏成品无关。
#
# 【如果你想公开开发过程】（比如作为「AI 协作开发实录」展示，这其实是个不错的
#  项目亮点）：把下面这一行删掉就会上传。
#  但删之前请先自己翻一遍 .workbuddy/memory/*.md，
#  确认里面没有本机绝对路径（形如 C:\Users\10955\...）、密钥或私人信息。
.workbuddy/


# ------------------------------------------------------------------------------
# 6. 操作系统 / 编辑器自动生成的杂物
# ------------------------------------------------------------------------------
# 这些是 Windows 资源管理器、macOS 访达、以及各种编辑器自己偷偷生成的隐藏文件，
# 跟项目内容无关，几乎所有开源项目都会忽略它们。
Thumbs.db
ehthumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/
*.swp
*.swo
*~


# ------------------------------------------------------------------------------
# 7. Node.js 相关【预防性规则 —— 当前项目其实用不到】
# ------------------------------------------------------------------------------
# 本项目目前「零 npm 依赖」：没有 package.json，也没有 node_modules 文件夹。
# 但只要你以后执行过一次 npm install（哪怕只是临时装个 serve 来起本地服务器），
# 就会冒出一个几万个文件、动辄上百 MB 的 node_modules。
# 那东西一旦误传上 GitHub 会非常难清理，所以提前挡在这里。
# 当前不会忽略任何已存在的文件，纯粹是「上保险」。
node_modules/
npm-debug.log*
yarn-error.log*
.npm/
package-lock.json.bak


# ------------------------------------------------------------------------------
# 8. Python 缓存
# ------------------------------------------------------------------------------
# tools/ 下有 gen_covers.py 和 gen_icon.py 两个 Python 脚本。
# Python 运行时会自动生成 __pycache__ 缓存目录和 .pyc 文件，
# 它们是机器码缓存，换台电脑就失效，没有任何上传价值。
__pycache__/
*.py[cod]
.venv/
venv/


# ------------------------------------------------------------------------------
# 9. 环境变量与密钥【安全预防】
# ------------------------------------------------------------------------------
# 当前项目没有这类文件。但如果将来接入了需要密钥的服务
# （比如统计分析、排行榜后端），密钥务必写进 .env 文件而不是硬编码进 src/，
# 这样才不会被这条规则漏掉而意外公开。
.env
.env.*
*.key
*.pem


# ------------------------------------------------------------------------------
# 10. 早期内部草稿文档【不公开】
# ------------------------------------------------------------------------------
# 以下文件是早期开发过程中的内部草稿与过程记录，与游戏本体的客观说明无关，
# 不纳入公开仓库。（文件仍保留在本地磁盘，仅供作者个人留存。）
docs/release/
design/rebrand/
docs/_internal-draft-en.md

# ------------------------------------------------------------------------------
# 11. 本地临时目录（非源码）
# ------------------------------------------------------------------------------
# 临时生成的拷贝目录，不属于游戏源码，不入库。
_deploy/

# ------------------------------------------------------------------------------
# 12. 视觉素材规范草稿【不公开】
# ------------------------------------------------------------------------------
# 这两份是封面 / 图标 / 截图的制作规范草稿，不属于游戏本体的客观说明，不纳入公开仓库。
docs/icon-spec.md
docs/screenshot-list.md
